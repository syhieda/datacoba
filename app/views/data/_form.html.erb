<%= form_with(model: datum) do |form| %>
  <% if datum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(datum.errors.count, "error") %> prohibited this datum from being saved:</h2>

      <ul>
        <% datum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nama, style: "display: block" %>
    <%= form.text_field :nama %>
  </div>

  <div>
    <%= form.label :data_nama, style: "display: block" %>
    <%= form.text_field :data_nama %>
  </div>

  <div>
    <%= form.label :, style: "display: block" %>
    <%= form.text_field : %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
